import Link from "next/link"; import { topics } from "./topics"; import { cases } from "../cases";
export default function Archive(){return <main><nav className="site-nav"><Link className="logo" href="/"><b>D/A</b><span>DEEP IN THE ABYSS<small>Conspiracy archive</small></span></Link><div><Link href="/">Live matches</Link></div></nav><header className="archive-hero"><p className="label">Evergreen archive</p><h1>{topics.length} rabbit holes.<br/>One evidence standard.</h1><p>Famous conspiracies, documented government programs, unexplained events, religious mysteries, cryptids, UFOs, lost civilizations, and internet folklore.</p></header><section className="archive-grid">{topics.map(([title,category,status],index)=>{const complete=cases.find((item)=>item.title.toLowerCase().includes(title.toLowerCase())||title.toLowerCase().includes(item.title.toLowerCase()));return complete?<Link href={`/cases/${complete.slug}`} key={title}><span>{String(index+1).padStart(2,"0")}</span><small>{category}</small><h2>{title}</h2><p>{status}</p><b>Read complete file →</b></Link>:<article key={title}><span>{String(index+1).padStart(2,"0")}</span><small>{category}</small><h2>{title}</h2><p>{status}</p><b>Research queued</b></article>})}</section><footer><Link className="logo" href="/"><b>D/A</b><span>DEEP IN THE ABYSS</span></Link><p>The archive grows by evidence quality, not automatic filler.</p><span>© 2026</span></footer></main>}
